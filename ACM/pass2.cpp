/*Hugo David Franco Avila*/
#include <iostream>
#include <bits/stdc++.h>

using namespace std;

string s;

string check(string s){
	vector<int> indice;
	int prim = 0, sec = 0, ter = 0, i =0, nump = 1;;
	string::iterator it1, it2, it3;
	v.push_back(s.size() - 1);
	for(i = s.size() - 2; i >= 0; i--){
		if(s[s.size() - 1] == s[i]){
			v.push_back(i);
		}
	}
	if(v.size() > 3){
		
	}
	for(
	if(ter == 0) return "Just a legend";
	
}

int main(){
	string sub;
	cin >> s;
	sub = pass(s);
	
	
	return 0;
}